#include "main.h"
#include <stdio.h>

/**
 * main - function that takes a pointer to an int as parameter and updates the value it points to to 98.
 * Retern: Always
 */
void reset_to_98(int *n)
{
	*n = 98;
}
